# FLOATER adatived from Roberta model 

## Install:

```base
# Create a new virtual environment
conda create --name floater-roberta python=3.7
# Install pytorch

# Install floater
pip install -e .
```
